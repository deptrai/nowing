"""Nowing MCP server.

Exposes Nowing's scrapers, knowledge base, and workspaces to MCP clients
(Claude Code, Cursor, Claude Desktop). Connects to a Nowing backend over its
REST API, authenticating with a Nowing API key.
"""

__version__ = "0.1.0"
