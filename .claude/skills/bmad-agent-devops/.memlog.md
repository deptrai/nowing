---
topic: DevOps agent for nowing (rebuilt from chainlens original)
goal: Memory agent, Dokploy-centric, replaces broken chainlens copy
updated: 2026-07-25T18:07
---

- (decision) Source chainlens agent (bmad-agent-devops) ships only SKILL.md; all 7 references/*.md links are dead. User chose full rebuild to nowing pattern over raw copy.
- (decision) Agent type: Memory agent (sanctum persists project IDs, app IDs, past incidents, infra quirks across sessions).
- (decision) Capabilities: full original set (deploy, env, domain, infra, troubleshoot, status) minus generic Supabase self-hosted, replaced by Postgres/pgvector self-hosted (nowing stack: pgvector/pgvector:pg17, redis, caddy, zero-cache, per docker/docker-compose.yml).
- (decision) Primary infra interface is Dokploy MCP tools (already connected in session) rather than SSH+bash credential collection from chainlens original.
- (decision) First Breath style: configuration (focused domain tool, warm but efficient) — confirmed by user, not calibration.
- (decision) customize.toml override surface: declined (default No for memory agents) — sanctum is the customization surface.
- (decision) Added 2 capabilities the vision implied but user hadn't named: Backup & DR guardian (nowing has scripts/backup/pg_backup.sh + Dokploy Backups tab but README notes no cron scheduled by default — real gap); Incident memory folded into troubleshoot (sanctum remembers past incidents/root causes, not a separate capability).
- (note) Hard-won institutional knowledge to preserve in CREED.md persistent facts: application-update is the only reliable Dokploy config tool (application-saveBuildType returns 400); NEXT_PUBLIC_* vars are baked at build time (must be Docker ARG/ENV in builder stage, runtime env cannot override); Traefik may not be running on fresh VPS (system nginx often blocks 80/443); Docker DNS is dynamic (use resolver 127.0.0.11 valid=10s + set $var, not static upstream blocks); production env should use CHANGE_ME placeholders never null; always verify golden path via curl internally then externally via DNS.
- (assumption) Directory name: bmad-agent-devops (mirrors sibling bmad-agent-dev/bmad-agent-pm convention already in nowing, despite template-substitution-rules.md reserving bmad- prefix for official BMad — nowing's existing user-created agents already use it, so following local convention over the generic rule).
- (decision) Agent name left blank (name = "" in customize.toml) — memory agent names itself at First Breath, not at build time. Icon carried forward from chainlens original: 🚀.
- (decision) User approved minimal draft as-is (name blank/First-Breath-named, icon 🚀, 8 capabilities, configuration-style First Breath, no override surface). Proceeding to full build.
- (decision) Evolvable = True: owner có thể dạy capability mới sau khi ship (secret rotation, canary check, runbook mới). Emit references/capability-authoring.md + section Learned trong CAPABILITIES.md. Lý do: agent ops tích lũy tri thức vận hành theo thời gian, không phải tập năng lực đóng.
- (decision) Evolvable = True: agent ops tich luy tri thuc van hanh theo thoi gian, nen emit capability-authoring.md + Learned section trong CAPABILITIES.md.
- (event) Emit xong 25 file: SKILL.md, customize.toml, 11 references (8 capability + first-breath + memory-guidance + capability-authoring + canon), 6 assets sanctum template, 2 scripts + 2 test files.
- (event) Lint gate: scan-scripts pass (high=0, con 4 medium la argparse/json.dumps tren wake.py+init-sanctum.py - khong ap dung cho script scaffolding co giao dien positional). Tests: wake.py 13/13 pass, init-sanctum.py 30/30 pass tren chinh skill bundle that.
- (note) scan-path-standards bao 1 high false-positive: .memlog.md o skill root bi coi la prompt file. Dung vi tri theo build-process.md (memlog nam canh SKILL.md), khong di chuyen.
- (gap) Eval beat chua chay: bmad-eval-runner (trigger/baseline mode) van con mo, offer cho user o handoff.
