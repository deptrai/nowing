# Epic 13 P0 Code-Review Closeout — 2026-08-07

## Verdict

**PASS** for P0 must-fix items across Stories 13.1, 13.2, 13.3 (with remaining P1/P2 follow-ups).

## 13.1

| ID | Finding | Status on HEAD |
|----|---------|----------------|
| M1 | Celery embedding RLS / missing workspace_id | **Fixed** — task takes `workspace_id`, sets context before SELECT |
| M2 | CAS `WHERE version == new_version` | **Fixed** — upsert uses `with_for_update` + `expected_version`; no broken UPDATE |

## 13.2

| ID | Finding | Status |
|----|---------|--------|
| M1 | No outbox retry worker | **Fixed** — `process_canonical_persist_outbox` + beat every 2 min |
| M2 | CAS upsert | **Fixed** (same as 13.1) |
| M3 | source_record_id fallback collision | **Fixed** — BDS/Jobs use content hash fallback |
| S2 | phone_key unkeyed SHA-256 | **Fixed** — HMAC-SHA256 via `CANONICAL_PII_DIGEST_KEY` |

## 13.3

| ID | Finding | Status on HEAD |
|----|---------|----------------|
| M1 | view_sources 404 | **Fixed** — `GET /canonical-entities/{id}/sources` + href under `/api/v1/...` |
| M2 | FTS english vs simple | **Fixed** — index uses `to_tsvector('simple', search_text)` |

## AD-29 / AD-30 / AD-31

**Accepted 2026-08-07** on Architecture Spine (owner request).

## Remaining (non-P0)

- 13.2 S1 residual notes, W2 revert title/search_text, harder dedup fixtures
- 13.3 pagination, filter validation, canonical_data preview
- Prefer dedicated BYPASSRLS role for outbox drain in production ops

## Sprint status

`13-1`, `13-2`, `13-3` → `done` after this closeout commit.
