# Red-Phase ATDD Unit Test Scaffolds — 9-6c

> Generated by `bmad-testarch-atdd` (Step 4). These are **red-phase** scaffolds: test titles only, no assertions. Bodies should be implemented by `bmad-dev-story` or `bmad-nowing-integration-test`.

## Pure Function Unit Tests (`tests/unit/services/test_revalidation_helpers.py`)

For functions `app/services/memory/revalidation_service.py:_extract_text` and `:_normalize`. These can be unit-tested without DB.

### `_extract_text(output, capability_name)`

```python
import pytest

pytestmark = [pytest.mark.unit]


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_extract_text_returns_answer_from_basemodel():
    """_extract_text should return the 'answer' field from a Pydantic BaseModel output."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_extract_text_joins_items_list_from_basemodel():
    """_extract_text should join a list of 'items' into JSON-serialized lines."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_extract_text_falls_back_to_json_dump_for_basemodel_without_answer_or_items():
    """_extract_text should return JSON dump for a BaseModel without 'answer' or 'items'."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_extract_text_returns_answer_from_plain_object_model_dump():
    """_extract_text should call model_dump() on a plain object with the method."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_extract_text_returns_answer_from_dict():
    """_extract_text should return the 'answer' field from a dict output."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_extract_text_joins_items_from_dict():
    """_extract_text should join a list of 'items' from a dict output."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_extract_text_returns_str_for_non_dict_non_model_output():
    """_extract_text should fall back to str(output) for arbitrary objects."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_extract_text_handles_model_dump_exception_gracefully():
    """_extract_text should not crash if model_dump() raises."""
```

### `_normalize(text)`

```python

@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_normalize_casefolds_and_collapse_whitespace():
    """_normalize should lowercase and collapse runs of whitespace to a single space."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_normalize_strips_leading_and_trailing_whitespace():
    """_normalize should strip leading and trailing whitespace."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_normalize_handles_empty_string():
    """_normalize should return empty string for empty input."""
```

## Service-Level Unit Tests (mocked DB)

For `RevalidationService` with mocked session/repository. These are higher-risk unit tests that may overlap with integration tests; prefer integration for DB-heavy logic.

```python

@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_revalidate_raises_not_revalidatable_for_missing_recipe():
    """RevalidationService.revalidate should raise not_revalidatable when source_capability is None."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_revalidate_raises_not_revalidatable_for_non_scraper_source_type():
    """RevalidationService.revalidate should raise not_revalidatable when source_type is not SCRAPER_RUN."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_revalidate_bumps_confidence_on_match():
    """RevalidationService.revalidate should bump confidence when content matches."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_revalidate_creates_memory_version_on_mismatch():
    """RevalidationService.revalidate should create MemoryVersion when content mismatches."""


@pytest.mark.skip("RED PHASE: implement after 9-6c")
def test_revalidate_calls_gate_then_execute_then_charge():
    """RevalidationService.revalidate should call gate_capability, execute_with_context, charge_capability in order."""
```

## Handoff Notes

- Unit tests for `_extract_text` and `_normalize` can be implemented immediately and will likely pass (code already exists).
- Service-level unit tests are optional; 9-6c is primarily an **integration/E2E gate** story. Prioritize `bmad-nowing-integration-test` (Step 4.6) for the E2E flow.
- When implementing, remove `@pytest.mark.skip` and replace with real assertions.
