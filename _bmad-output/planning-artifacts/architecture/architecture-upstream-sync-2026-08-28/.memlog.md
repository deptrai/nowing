---
scope: Kiến trúc đồng bộ upstream SurfSense cho Walmart, Indeed, Reddit scrapers và Timeline/Thinking Steps UI
purpose: Xác định invariants và quy tắc port code từ upstream surfsense_* sang nowing_* mà không phá vỡ kiến trúc hiện tại của Nowing
altitude: feature
updated: 2026-08-28T19:25
---

- (direction) Tận dụng code upstream SurfSense nhưng phải phù hợp với kiến trúc Nowing hiện tại
- (decision) AD-1: Paradigm là Layered Scraper Platform với namespace adapter — port file-by-file từ surfsense_* sang nowing_* thay vì merge git
- (decision) AD-2: Browser lifecycle pool chung tối thiểu hóa process leak — mọi scraper dùng Playwright phải đi qua crawler lifecycle manager
- (decision) AD-3: MCP tool schema phải mirror backend capability schema — một tool = một endpoint + một Pydantic request/response model
- (decision) AD-4: Frontend TimelineActivityIndicator là controlled React component kế thừa từ streaming activity-journal state — không dùng pixel-grid loader nữa
- (decision) AD-5: Reasoning episode auto-scroll dùng IntersectionObserver + refs — scrollIntoView với behavior 'smooth' tránh giật viewport
- (constraint) Giữ nguyên toàn bộ scraper Việt Nam hiện có (batdongsan, topcv, itviec, cafef, vietstock, chotot, masothue) và các test tương ứng
- (assumption) Walmart upstream thêm next_data.py — Nowing chưa có file này, cần port và xem xét tích hợp vào scraper
- (assumption) Indeed upstream tách thành indeed_jobs với fetch.py, parsers.py, url_resolver.py — Nowing chỉ có scraper.py, cần tái cấu trúc
- (assumption) Reddit upstream hỗ trợ community-only scrape (search_queries rỗng) — cần kiểm tra url_resolver và parser
- (assumption) Timeline/Activity Journal upstream refactor lớn — activity-journal.ts và activity_journal.py cần đồng bộ state client-server
- (version) upstream/main: PR #1692, #1686, #1614, #1605 — caaca4400 refactor(activity) là merge base gần nhất
- (question) Có cần cập nhật database schema hay migrations từ upstream (alembic versions 175-186) không?
- (question) upstream Walmart Reviews capability chưa có trong Nowing — có đưa vào MCP và web catalog ngay không?
- (event) Fast path chosen by user; drafting architecture spine from memlog
- (decision) AD-6: Vòng đời scraper (fetch → parse → resolve_url → build_result) tuân theo platform contract chung — mỗi platform package phải xuất fetch, parsers, schemas, scraper, url_resolver
- (decision) AD-7: Capability executor là thin adapter — chỉ wrap platform scraper, chuyển đổi Pydantic schema, và ghi credit metering, không chứa logic cào
- (decision) AD-8: Web UI timeline dựa trên streaming journal entries, reasoning item render container tự scroll mà không scroll cả chat viewport
- (decision) AD-9: Namespace import map — mọi tham chiếu surfsense_ trong upstream phải đổi thành nowing_ khi port, bao gồm package path, class name, logger, environment variable, và URL prefix
- (decision) AD-10: [ASSUMPTION] Chỉ port delta từ upstream PR #1614, #1605, #1692, #1686 — không merge toàn bộ upstream/main để tránh xung đột namespace
- (decision) AD-11: [ASSUMPTION] Chạy pytest cho các platform walmart/indeed/reddit mới trước khi merge; web build (pnpm build) phải pass trước khi đánh dấu hoàn thành
- (decision) AD-12: Deferred: OpenTelemetry / Grafana LGTM, Daytona sandbox, credit migration — không port theo SPEC non-goals
- (constraint) Không thay đổi schema DB hoặc model auth/credit hiện tại,
- (constraint) Tất cả UI strings phải hỗ trợ i18n tiếng Việt, kỹ thuật identifiers giữ nguyên tiếng Anh
- (constraint) Tất cả code port không được chứa tham chiếu surfsense_* còn sót
- (event) ARCHITECTURE-SPINE.md drafted from memlog; ready for reviewer gate
