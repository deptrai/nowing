---
topic: Kết nối Nowing ↔ XActions: control plane (MCP x_scrape) + data plane (Redis Stream) + artifact; adopted companion = ARCHITECTURE-SPINE epic-level ở architecture-Nowing-2026-09-13
updated: 2026-09-13T18:50
---

- (note) Nguồn: ARCHITECTURE-SPINE.md (architecture-Nowing-2026-09-13) đã finalize — adopted companion, giữ AD-1..10 + AD-SOC-1..11. Đây là SPEC kernel cho cùng scope: contract What/Why, spine giữ How/invariants.
- (capability) CAP-1 — Unified scrape control: nowing ra lệnh scrape bất kỳ platform/action nào qua MỘT tool duy nhất. intent: nowing gọi 1 endpoint MCP generic thay vì biết 150+ per-platform tool. success: mọi SocialMonitoredTarget platform (gồm VN: chotot/shopee/topcv/masothue/batdongsan/vietnamworks/linkedin/b2b/tiktok/zalo...) dispatch thành công, không tool_not_found.
- (capability) CAP-2 — Bulk data ingest qua stream: dữ liệu scrape chảy liên tục cường độ cao, không qua MCP response. intent: mọi crawler (kể cả VN/non-social) emit thin event vào stream:social:raw_posts; nowing consume bằng consumer group. success: VN crawler emit được event; consumer tạo social_posts + Lead không drop (0% loss trên event hợp lệ).
- (capability) CAP-3 — Action discovery động: nowing resolve platform→action từ XActions thay vì hard-code. intent: nowing query catalog action canonical khi startup. success: thêm/đổi action phía XActions không cần sửa nowing; unsupported action → mark target thay vì retry vô hạn.
- (capability) CAP-4 — Delta/incremental crawl: chỉ cào phần gap, không re-crawl toàn bộ. intent: nowing định target + interval, XActions tự resume từ checkpoint. success: run 2 của cùng target cào ít hơn run 1 (early-termination on all-duplicate page); không duplicate vào DB.
- (capability) CAP-5 — Multi-tenant isolation qua args, không qua session: nhiều workspace/target dùng chung 1 consumer 'nowing'. intent: tenant phân biệt bằng args.accountId + context{targetId,workspaceId} trong call, không phải MCP session riêng. success: N workspace ingest đồng thời không leak RAM/session; mỗi post gán đúng workspace_id.
- (capability) CAP-6 — Governed rate + account protection: throttle theo proxy sống, hibernate khi WAF. intent: nowing tôn trọng governor/quota, phản ứng error envelope thống nhất. success: XACT_4291→retry sau retry_after; hibernation→pause target; không die account hàng loạt.
- (constraint) Transport contract đã chốt (kế thừa AD-SOC-4): MCP streamable-http :3001 = control, Redis Stream = data. Không re-decide REST — REST /api/platform cần JWT user, /api/ai cần x402 payment, đều không hợp service-to-service.
- (constraint) Single-writer stream: chỉ XActions xAdd stream:social:raw_posts; nowing chỉ consume. Nowing ingest_raw_post_to_stream hiện tại là nguồn ghi thứ 2 → phải loại bỏ.
- (constraint) Wire schema = snake_case theo SocialPostEvent của nowing consumer: {platform, external_post_id, content_snippet, author_id/name, post_url, crawled_at, storage_ref, target_id, workspace_id, schema_version}. Thiếu workspace_id/content → drop im lặng.
- (constraint) Action name = canonical từ x_actions_list (search_listings/search_products/search_jobs/detail/company_profile...), KHÔNG nowing tự đặt — PLATFORM_TOOL_MAP hiện sai lệch (posts/lookup/company không tồn tại).
- (constraint) x_crawl_post fallback phải truyền platform; x_scrape.args là object lồng không phẳng.
- (constraint) MCP client = loop-scoped cache (Celery new_event_loop+close mỗi task), không proc-singleton.
- (nongoal) Nowing không re-implement scraper, không quản proxy/fingerprint/signer (AD-SOC-1/2/3) — đó là domain XActions.
- (nongoal) Không build REST internal endpoint mới; không thay MCP bằng REST (transport đã chốt, REST không tăng throughput vì cùng governor).
- (nongoal) Không truyền full content/payload qua stream thin event — chỉ content_snippet ≤4000 + storage_ref; full qua artifact.
- (nongoal) Không per-user MCP session, không per-workspace X-Consumer-Id — 1 consumer 'nowing' duy nhất.
- (assumption) ingest_raw_post_to_stream của nowing chỉ là bridge tạm chờ XActions emit — không còn caller cần sau khi AD-3/AD-4 áp dụng. Cần xác nhận.
- (question) Q1: artifact >100 records — nowing đọc qua shared volume XACTIONS_ARTIFACT_ROOT hay presigned URL?
- (question) Q2: Redis consumer group name + DLQ stream:social:failed + schema_version — thống nhất 2 đầu chưa?
- (question) Q3: XActions MCP daemon topo prod — cùng network (xactions:3001 http) hay cross-cluster cần TLS?
- (direction) Phasing: P0 nowing vá ngay (wire fallback_crawl_post đúng platform + x_actions_list graceful) để VN target chạy post-detail. P1 XActions ship x_scrape + x_actions_list coverage + AbstractCrawler stream hook. P2 nowing bỏ PLATFORM_TOOL_MAP + ingest_raw_post_to_stream + canonical action matrix.
- (event) spec distilled from finalized spine; adopted companion referenced, not edited.
- (event) self-validate Pass1 coherence: 6 CAP đều có intent+success WHAT-only; 7 constraints đều bend design; 4 non-goals explicit; success signal testable end-to-end; CAP IDs stable; lean. PASS.
- (event) self-validate Pass2 preservation: mọi load-bearing claim từ spine + code-discovery đã land — x_scrape missing, 15 crawler no-xAdd, dual-writer stream, snake_case casing, missing workspace_id/content_snippet, loop-scoped client, flat-vs-nested args, action-name mismatch, fallback thiếu platform, scoped idempotency, governor 60RPM, ACL checkpoint. Spine adopted làm companion. PASS.
- (event) bmad-review Epic 36: 4 lens (adversarial 17, edge-case 29, structure 10, prose 14). Đã áp dụng: gộp 36.5+36.6→36.6, thêm Story Execution & Dependency Matrix + hard deploy-order + feature flags, siết AC cho edge cases (fallback no-URL→unsupported+is_active=False, loop-scoped WeakKeyDict + serialized call_tool + reconnect, adapter trả TaskBehavior enum không gọi task.retry, retry_after clamp 5..3600, single-writer gate flag + transition dual-write, SocialPostEvent content_snippet alias, DLQ XACK + schema_version + repr fallback, matrix 422 validation). Epic 36 còn 6 stories.
- (event) apply-findings hoàn tất: đối chiếu 70 findings × 4 lens với Epic 36 — tất cả đã land trừ consumer-lag-alerting (adversarial), vừa thêm vào Story 36.5 AC (XINFO GROUPS/XPENDING lag probe + alert khi gần MAXLEN). Epic 36 = 6 stories, dependency matrix + deploy-order + feature flags.
