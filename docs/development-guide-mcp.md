# Hướng dẫn phát triển - MCP Server

**Ngày tạo:** 2026-07-21 16:59:34

```bash
cd surfsense_mcp
uv sync
uv run python -m mcp_server.selfcheck
uv run python -m mcp_server
```

Cấu hình `SURFSENSE_BASE_URL` và `SURFSENSE_API_KEY` trong môi trường.

---

_Tài liệu được tạo bởi BMAD Method `document-project` workflow_
