# Hướng dẫn phát triển - Browser Extension

**Ngày tạo:** 2026-07-21 16:59:34

```bash
cd surfsense_browser_extension
pnpm install
pnpm dev        # Plasmo dev mode
pnpm build
pnpm package
```

Load `build/` hoặc `dist/` vào trình duyệt ở chế độ developer.

---

_Tài liệu được tạo bởi BMAD Method `document-project` workflow_
