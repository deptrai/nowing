baseline_ratified: false

thresholds:
  # Provisional per-mode p95 e2e latency ceilings (Story 4.8g).
  per_mode:
    speed:
      max_p95_e2e_ms: 15000
    balanced:
      max_p95_e2e_ms: 30000
    quality:
      max_p95_e2e_ms: 60000
  max_degraded_rate: 0.10
  max_engine_unavailable_rate: 0.05
