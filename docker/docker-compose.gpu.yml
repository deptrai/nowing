services:
  backend:
    deploy:
      resources:
        reservations:
          devices:
            - driver: ${NOWING_GPU_DRIVER:-nvidia}
              count: ${NOWING_GPU_COUNT:-1}
              capabilities:
                - gpu

  celery_worker:
    deploy:
      resources:
        reservations:
          devices:
            - driver: ${NOWING_GPU_DRIVER:-nvidia}
              count: ${NOWING_GPU_COUNT:-1}
              capabilities:
                - gpu

  celery_beat:
    deploy:
      resources:
        reservations:
          devices:
            - driver: ${NOWING_GPU_DRIVER:-nvidia}
              count: ${NOWING_GPU_COUNT:-1}
              capabilities:
                - gpu
