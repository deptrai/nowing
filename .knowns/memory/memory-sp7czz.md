---
id: sp7czz
title: Session working context
layer: project
category: context
status: proposed
tags:
  - session
  - working-context
createdAt: '2026-08-02T03:34:20.679Z'
updatedAt: '2026-08-02T03:34:20.679Z'
---

Hi Luisphan, File snippet trước bị lệch path (sandbox sync issue). Đã lưu lại ở: apps/api/scripts/nowing-sse-contract-snippet.py Dưới đây paste đầy đủ nội dung — có thể copy ra file .py và chạy standalone. #!/usr/bin/env python3 """ ChainLens /api/v1/search SSE contract regression snippet for Nowing. This file is self-contained: it parses the ChainLens SSE wire format, extracts the final answer, sources, and costDollars from the terminal `done` frame, and includes a sample stream with all requested lifecycle events (progress, evidence_ready, researchComplete, synthesizing, done.usage, error). Usage: # 1. Run against the live sample wire (no API key required): python nowing-sse-contract-snippet.py # 2. Run against a real ChainLens endpoint: export CHAINLENS_API_URL="https://research- api.chainlens.net" export CHAINLENS_API_KEY="your-key" python nowing-sse-contract-snippet.py --live "compare postgres vs mongodb" Dependencies: - requests (only for --live mode) """ import argparse import json import os import sys from typing import Any def parse_sse_stream(wire: str) -> list[dict[str, Any]]: """Parse a ChainLens SSE stream into a list of payload objects. The wire format is data-only frames (no `event:` lines), each frame optionally prefixed with an `id:` line by NestJS: id: 1\n data: {"type":"init", ...}\n\n Unknown/future event types are ignored but preserved in the returned list so the caller can decide what to do with them. """ events: list[dict[str, Any]] = [] for frame in wire.split("\n\n"): if not frame.strip(): continue data_lines = [line for line in frame.splitlines() if line.startswith("data:")] for line in data_lines: payload = line.removeprefix("data:").strip() if not payload: continue try: events.append(json.loads(payload)) except json.JSONDecodeError: pass return events def consume_stream(events: list[dict[str, Any]]) -> dict[str, Any]: """Consume a parsed SSE stream and extract the answer, sources, done metadata. Returns: { "events": [...], # all parsed events "answer": str | None, # final text block data "sources": list, # source chunks "done": dict | None, # terminal done event "error": str | None, # inline error data "costDollars": float | None, } """ blocks: dict[str, dict[str, Any]] = {} result: dict[str, Any] = { "events": events, "answer": None, "sources": [], "done": None, "error": None, "costDollars": None, } for ev in events: ev_type = ev.get("type") if ev_type == "block" and isinstance(ev.get("block"), dict): block = ev["block"] blocks[block.get("id")] = block elif ev_type == "updateBlock" and isinstance(ev.get("patch"), list): block_id = ev.get("blockId") block = blocks.get(block_id) if block: for op in ev["patch"]: if isinstance(op, dict) and op.get("op") == "replace" and op.get("path") == "/data": block["data"] = op.get("value") elif ev_type == "done": result["done"] = ev usage = ev.get("usage") or {} cost = usage.get("costDollars") if isinstance(cost, (int, float)): result["costDollars"] = float(cost) elif ev_type == "error": result["error"] = ev.get("data") for block in blocks.values(): if block.get("type") == "text" and isinstance(block.get("data"), str): result["answer"] = block["data"] elif block.get("type") == "source" and isinstance(block.get("data"), list): result["sources"].extend(block["data"]) return result SAMPLE_WIRE = """\ id: 1 data: {"type":"init","data":"Stream connected","requestId":"req-sample-0000"} id: 2 data: {"type":"progress","requestAcceptedAt":1700000000000,"firstProgressAt":1700000000050} id: 3 data: {"type":"evidence_ready","evidenceReadyAt":1700000000100,"sources":[{"title":"PostgreSQL: Documentation","url":"https:// www.postgresql.org/docs/","content":"PostgreSQL is a relational database..."},{"title":"MongoDB Documentation","url":"https://www.mongodb.com/ docs/","content":"MongoDB is a document-oriented database..."}]} id: 4 data: {"type":"researchComplete"} id: 5 data: {"type":"synthesizing"} id: 6 data: {"type":"block","block": {"id":"source-1","type":"source","data":[{"title":"PostgreSQL:Documentation","url":"https://www.postgresql.org/ docs/","content":"PostgreSQL is a relational database..."}, {"title":"MongoDB Documentation","url":"https:// www.mongodb.com/docs/","content":"MongoDB is a document- oriented database..."}]}} id: 7 data: {"type":"block","block": {"id":"text-1","type":"text","data":""}} id: 8 data: {"type":"updateBlock","blockId":"text-1","patch": [{"op":"replace","path":"/data","value":"PostgreSQL and MongoDB target different workloads."}]} id: 9 data: {"type":"updateBlock","blockId":"text-1","patch": [{"op":"replace","path":"/data","value":"PostgreSQL and MongoDB target different workloads. PostgreSQL [1] favours relational integrity, while MongoDB [2] favours flexible document schemas."}]} id: 10 data: {"type":"done","usage": {"requestedMode":"balanced","resolvedMode":"balanced","totalTokens":1280,"promptTokens":900,"completionTokens":380,"costDollars":0.00123},"requestedMode":"balanced","resolvedMode":"balanced","chatId":"chat-sample-0000","webUrl":"https:// v2.chainlens.app/c/chat-sample-0000"} """ SAMPLE_ERROR_WIRE = """\ id: 1 data: {"type":"init","data":"Stream connected","requestId":"req-err-0000"} id: 2 data: {"type":"error","data":"Missing query"} """ def demo(wire: str, label: str) -> None: print(f"=== {label} ===") events = parse_sse_stream(wire) for ev in events: print(f" event: {ev.get('type')}") result = consume_stream(events) print(f" answer: {result['answer']!r}") print(f" sources: {len(result['sources'])} chunks") print(f" done: {json.dumps(result['done'], indent=2) if result['done'] else None}") print(f" costDollars: {result['costDollars']}") print(f" error: {result['error']!r}") print() def live_query(query: str) -> None: import requests api_url = os.environ.get("CHAINLENS_API_URL", "https:// research-api.chainlens.net") api_key = os.environ.get("CHAINLENS_API_KEY", "") headers: dict[str, str] = {"Content-Type": "application/ json"} if api_key: headers["Authorization"] = f"Bearer {api_key}" body = { "query": query, "sources": ["web"], "optimizationMode": "balanced", "history": [], "tier": "ask", } resp = requests.post( f"{api_url}/api/v1/search", headers=headers, json=body, stream=True, timeout=120, ) resp.raise_for_status() raw = b"" for chunk in resp.iter_content(chunk_size=None): raw += chunk wire = raw.decode("utf-8", errors="replace") events = parse_sse_stream(wire) result = consume_stream(events) print(json.dumps(result, indent=2, default=str)) def main() -> None: parser = argparse.ArgumentParser(description="ChainLens SSE contract regression snippet") parser.add_argument( "--live", metavar="QUERY", help="Run a live query against CHAINLENS_API_URL (requires requests + API key)", ) args = parser.parse_args() if args.live: live_query(args.live) return demo(SAMPLE_WIRE, "Happy path with done.usage costDollars") demo(SAMPLE_ERROR_WIRE, "Inline error path") if __name__ == "__main__": main() Cách chạy: python3 apps/api/scripts/nowing-sse-contract-snippet.py Kết quả mẫu: === Happy path with done.usage costDollars === event: init event: progress event: evidence_ready event: researchComplete event: synthesizing ... costDollars: 0.00123 ────────────────────────────────────────────────────────────── 2. Full-pipeline cost aggregation Writer-only cost đã confirmed. Task 6 (tổng hợp usage từ classifier/planner/researcher/writer/reflection) trong storie s/42-1-costdollars-in-sse.md vẫn là "LATER" / chưa có story/ PRD riêng. Plan từ phía ChainLens: • Tạo story + PRD follow-up (42-1b hoặc 42-3) trong tuần này. • Trình lên sprint planning tuần tới. • Dự kiến land: ~2–4 sprint (tức 2–4 tuần) sau khi benchmark và SearXNG/Brave/proxy pipeline ổn định, vì task này cần wire UsageLedgerService với PhaseTracker. Nowing có thể để lại margin 1.5–2.5× trên writer cost cho đến khi full-pipeline aggregation land. 3. Benchmark sạch hơn Đồng ý với plan SearXNG/Brave/proxy. Kết quả benchmark mới sẽ được gửi trong 24–48h sau khi config ổn định. Nowing chạy e2e benchmark từ phía mình ngay khi có số sạch — OK. 4. Quyết định bên Nowing • State A mặc định: xác nhận. • Story 9.5 backlog: xác nhận — chờ SCP phê duyệt account/ key model + pricing + quota. Thanks, ChainLens Dev Team.
