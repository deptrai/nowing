# E2E Report — Story 10.4

**Status:** ✅ PASS

## Scope

Smoke-test the `vn_bds.aggregate` capability through the real REST surface:
- Auth flow (e2e token mint)
- Workspace creation
- Capability execution

## Test Script

`/Users/luisphan/Documents/GitHub/nowing/_workspace/10-4/e2e_smoke.py`

## Commands Run

```bash
cd nowing_backend
uv run python tests/e2e/run_backend.py
# separate shell
cd /Users/luisphan/Documents/GitHub/nowing
uv run python _workspace/10-4/e2e_smoke.py
```

## Results

1. `POST /api/v1/workspaces` — created workspace successfully.
2. `POST /api/v1/workspaces/{id}/scrapers/vn_bds/aggregate` with unknown city (`Atlantis`):
   - HTTP 200
   - `degraded: true`
   - `degradation_reasons: ["batdongsan: unknown_city:Atlantis"]`
   - Validates the city-code mapping and graceful degradation path end-to-end.
3. `POST /api/v1/workspaces/{id}/scrapers/vn_bds/aggregate` with known city (`Hà Nội`):
   - HTTP 200
   - Response contains `items`, `degraded`, `degradation_reasons`, `source_breakdown`, `total_items`
   - Validates the REST wiring, authz, and orchestrator fan-out path end-to-end.

## Verdict

The `vn_bds.aggregate` capability is reachable through the real REST API, returns the expected output schema, and degrades gracefully. Story 10.4 is approved to merge.
