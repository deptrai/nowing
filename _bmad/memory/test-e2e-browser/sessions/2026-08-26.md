# Browser Pilot Session — 2026-08-26

## Story 25.4 Realtime Admin Telemetry Live Verification

- Started local stack: Postgres 5434, Redis 6380, Zero-cache 4848, FastAPI 8000, Next.js 3000.
- Hit alembic duplicate revision `202` (meeting_minutes vs ecommerce). Created a permanent fix: renamed the newer meeting_minutes migration to `233` and added merge `9a32642d01df`; `alembic heads` now returns one head. Initial verification used `Base.metadata.create_all()` because Docker/Postgres was running; final `alembic upgrade head` could not be run because Docker daemon stopped.
- Seeded superadmin `admin@nowing.net / AdminPass123!`, workspace 2, plus TokenUsage, BillingEvent, CreditPurchase rows.
- Logged in via Playwright MCP and navigated to `http://localhost:3000/admin/telemetry`.
- Initial dashboard rendered but LLM Cost and Gross Margin panels errored:
  - `AttributeError: key` in `_bucket()` because provider/model SQL columns are labeled `provider`/`model`, not `key`.
  - Fixed `_bucket()` to accept correct `key_attr` and coerce key to string for workspace/usage-type integer keys.
  - `AttributeError: key` for `worst_model_row.key` — fixed to `.model`; unit test mock updated from `key` to `model`.
- After backend restart, all four panels rendered cleanly:
  - Gross Margin: 93.00% margin, worst model `gpt-4o`.
  - LLM Cost: 900 tokens, by provider/model/usage/workspace tables and time-series chart.
  - Proxy Health: `custom` provider, degraded/dead, no credential leak.
  - Celery Queues: `nowing` queue length 0, healthy; overall unavailable because no worker.
- Console 0 errors after page settled.
- Captured screenshots: `story-25-4-telemetry-dashboard.png`, `story-25-4-telemetry-llm-cost.png`, `story-25-4-telemetry-celery.png`.
- Fixed `ProxyHealthPanel.tsx` success-rate display bug (`success_rate` was not multiplied by 100).
- Added E2E spec `nowing_web/tests/admin/telemetry.spec.ts` with mocked API; all 3 Playwright tests pass.
- Backend ruff + targeted pytest (19 passed), frontend tsc + biome + Playwright green.

## Story 25.5 Dynamic Scraper Rules & ReDoS Sandbox E2E Verification
- Ran Playwright E2E suite `nowing_web/tests/admin/scraper-rules.spec.ts`.
- All 7 tests passed green (18.5s):
  1. `[setup] authenticate`
  2. `renders the rules list and active rule details`
  3. `validates invalid CSS selector inline`
  4. `validates ReDoS regex inline`
  5. `trip circuit breaker updates status`
  6. `reset circuit breaker updates status`
  7. `polls the list every 5 seconds`
- Interception quirks resolved: CORS headers with explicit `Access-Control-Allow-Origin: http://localhost:3000` + `Access-Control-Allow-Credentials: true`, Zero Context mock `/zero/context`, and valid UUID v4 `userId`.

