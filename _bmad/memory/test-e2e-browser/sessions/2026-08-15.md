# Session Log — 2026-08-15

## Journeys Piloted
1. **Public Procurement & Tender Intelligence (Story 16.5 / Muasamcong e-GP v2.0):**
   - Verified authentication redirect from `/login` with credentials `e2e-test@nowing.net` / `E2eTestPassword123!`.
   - Verified New Chat creation `/dashboard/1/new-chat` -> `/dashboard/1/new-chat/168`.
   - Executed Vietnamese prompt: *"Tìm kiếm các gói thầu xây lắp tại Hà Nội trên 50 tỷ từ muasamcong và tóm tắt tiêu chí HSMT"*.
   - Verified SSE turn breakdown: "Reviewed" accordion rendering, tool action log modal, and responsive viewport at 1440x900.

## DOM Selectors & Patterns Confirmed
- Chat prompt textbox: `getByRole('textbox')` / `textbox [active]`.
- Action log modal: `button "Open agent action log"` / `button "Reviewed"` expands turn trace.
- Modal dismissal: Keyboard `Escape` cleanly closes overlays.
- Multi-Table Tabs: Scrollable tab bar with active tab query param `?table={id}` and inline rename/delete.
- Send & Export Hub: `button "Send & Export"` dropdown with CSV, Lark Base, Google Sheets, and share link.
- Field Mapping Modal: Inputs for Lark App/Table tokens and Google Spreadsheet ID with PII Masking toggle.

## Journeys Piloted
2. **Multi-Table Tabs & Send/Export Hub (Story 21.13):**
   - Verified dev servers & database health: Postgres (:5434), Redis (:6380), Zero-cache (:4848), Backend (:8000), Frontend (:3000).
   - Executed live browser login flow (`e2e-test@nowing.net`) and navigated to `/dashboard/1/leads`.
   - Verified creation of new tab (`BĐS Cao Cấp Hà Nội`), inline tab input, and reactive persistence.
   - Verified tab selection and URL synchronization (`?table=d0990c2a...`).
   - Verified Send & Export dropdown with CSV, Lark Base, Google Sheets, and Share Read-only Link.
   - Verified FieldMappingModal with automated 12-column schema mapping and PII Masking toggle.
   - Captured visual screenshot evidence and verified 0 console errors.


3. **Shopee Vietnam In-House Scraper & Price Normalization (Story 17.2):**
   - Verified New Chat creation `/dashboard/1/new-chat` -> `/dashboard/1/new-chat/169`.
   - Executed Vietnamese prompt: *"Tìm kiếm sản phẩm chuột không dây Logitech M331 trên Shopee và theo dõi biến động giá 90 ngày"*.
   - Verified SSE streaming, tool invocation accordion, price normalization in VNĐ, and markdown rendering.

4. **Telegram Public Web Preview Ingestion Engine & Channel Search (Story 22.1):**
   - Verified dynamic Scraper Playground at `/dashboard/1/playground/telegram/search`.
   - Verified form schema generation: `Channel Username` (required), Advanced parameters (keyword, intent, limit).
   - Executed zero-login public preview ingestion on live Telegram channel `telegram` (`https://t.me/s/telegram`).
   - Verified response metrics (Items: 1, Time: 1.6s, Cost: Free).
   - Verified parsed metadata: Channel info (title, description, subscribers: 9.82M), parsed messages array (message IDs, text, ISO timestamps, view counts: 1.55M, intent tags: 'news', extracted entities).
   - Verified Table and JSON viewer tabs, CSV export, and Copy JSON actions.

5. **Spatial Planning & Land Zoning GIS (Story 10.8):**
   - Navigated authenticated session to `/dashboard/1/realestate/land-zoning`.
   - Verified `LandZoningModal` opens automatically with title `Thẩm định quy hoạch đất đai`.
   - Filled lat/lng inputs and submitted real `POST /api/v1/workspaces/1/scrapers/realestate/zoning`.
   - Verified empty-state response renders without crash: `Không tìm thấy dữ liệu quy hoạch...` with Leaflet map.
   - Mocked 402 Insufficient Credits by patching `window.fetch`; verified error surfaces as `Không đủ credits để tra cứu quy hoạch.`
   - Wrote Playwright spec: `nowing_web/tests/realestate/land-zoning.spec.ts`.
   - Note: Full `pnpm test:e2e` run blocked because Playwright Chromium install hung on network/extraction; manual MCP drive passed.

6. **1-Click Reverse-ICP (Story 21.10):**
   - Diagnosed frontend dev server hang; restarted `nowing_web` (`next dev --turbopack`) to restore `localhost:3000`.
   - Fixed FastAPI route shadowing: moved `POST /workspaces/{workspace_id}/leads/reverse-icp` before the dynamic `/{lead_id}` routes in `nowing_backend/app/routes/leads_routes.py`.
   - Fixed `ReverseIcpService` to use the configured global Auto-mode LLM router instead of hardcoded `gpt-4o-mini` (`app/lead_intelligence/reverse_icp.py`).
   - Verified E2E on `/dashboard/1/leads`:
     - Opened `ReverseIcpModal`, filled `vinhomes.vn`, and received structured ICP result with buyer personas, search queries, negative keywords, and filter presets.
     - Verified `Áp dụng vào Bộ lọc` updates `sourceFilter` to `batdongsan` and search query from ICP (fixed platform-name mapping in `LeadsContent.tsx`).
     - `Tạo Tab Bảng Mới` calls `handleApplyIcpPresets` but does not yet create a new Multi-Table tab (placeholder).
     - `Mở Chat Săn Lead` navigates to `/dashboard/1/new-chat?q=<prompt>` but the new-chat page does not yet consume the `q` param to pre-fill the composer.
     - Started chat with prompt *"Phân tích ICP cho website vinhomes.vn"*; agent entered planning/thought state and began `web_crawler`-based ICP analysis.
   - Console errors persisted around Zero-cache reconnect and Turbopack HMR, non-blocking for this flow. Saved to `_bmad-output/test-artifacts/21-10-console-errors.log`.
   - Backend: `ruff check` and targeted `pytest tests/unit/lead_intelligence/test_reverse_icp_service.py tests/integration/routes/test_reverse_icp_route.py` pass (7/7).
   - Frontend: `pnpm tsc --noEmit` and `pnpm exec biome check components/leads/LeadsContent.tsx` pass.


