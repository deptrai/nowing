
## Story 37.6 — Pitch Portal Beacon E2E (verified, backend :4445 real)
- Portal `localhost:3000/pitch/44/{lead_uuid}` renders SSR meta: heading "Dành riêng cho {company}", sections hero/exec-summary/roi/booking, CTA → `/book/44/{lead}` (Story 37.3 link — loop connected).
- `POST /public/pitch/.../beacon` → 204 ×3 (open + heartbeats); `GET /meta` → 200.
- Session dedup verified in DB: 2 sessions = 2 `pitch_portal_view` rows; heartbeats UPDATE dwell in place (34s final). sections_viewed captured all 4 via IntersectionObserver.
- Dispatch attempted live: "no Telegram binding for user …" → fail-open, lock released (retries each beacon — intended).
- Crawler UA `facebookexternalhit` → 204 but ZERO rows (pre-DB filter). Bad lead → beacon 204 / meta 404.
- device_type='tablet' at 849px viewport (innerWidth<1024 rule).
- Artifact: `story-37-6-pitch-portal.png`.
- Env note: `python main.py` needs `.venv/bin/python` (system python3.11 lacks chonkie); backend takes ~60-90s to boot (model loading).

## Story 37.5 — Mini-Pitch Portal UI E2E (verified, backend :4445 real)
- Portal `/pitch/44/{lead}` renders generated content: per-lead headline "3 đòn bẩy… cho {company}", exec-summary card, exec cards (Thực trạng/Khoảng trống red|yellow border-l), ROI calc "3 sales → +15 cuộc hẹn, ~24.000.000 ₫", CTA → `/book/44/{lead}`, footer Decree-13 + opt-out link.
- Opt-out full UI loop (lead bc6cf694): footer link → /opt-out page (Decree 13 copy, company chip, destructive confirm btn) → submit → "Yêu cầu đã được ghi nhận" → DB consent_status=withdrawn → meta → 404.
- **BUG FOUND + FIXED**: `getPitchMeta` used `next:{revalidate:60}` — Next fetch Data Cache (`.next/dev/cache/fetch-cache`, persists across dev restarts) served stale 200 after opt-out → withdrawn lead's portal stayed live (Decree-13 gap). Worse: stale entry with pre-37.5 schema → `exec_cards.map` crash → error boundary. Fix: `cache:"no-store"` (commit pending). After fix both withdrawn leads render 404 page; fresh lead ff461979 renders normally.
- chrome_navigate only ACTIVATES tab if URL matches — use chrome_javascript `location.href=…` for real nav; `location.reload()` works.
- chrome_javascript `code` param returns undefined for expression evals — unreliable for DOM reads; use chrome_read_page.
- Leads 1d737323 + bc6cf694 (ws 44) now withdrawn — do not reuse for portal tests; fresh pool: ff461979, df955a04, 958cf995, 6f35ee3e, e6a7fd05, 356eb33c, 28ffa1bf.
