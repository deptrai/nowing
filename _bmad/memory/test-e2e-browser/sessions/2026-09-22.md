# Session 2026-09-22 — Story 39.1 DecisionService E2E

## Story 39.1 — Typed-Decision Layer (Jev) Telemetry Surface

**Scope:** backend-only story (`app/services/decision/` port + Jev/Mock backends + registry + `UsageType.DECISION`). No page/component/chat surface added — the ONLY human-visible surface is `/admin/telemetry` "Theo Loại sử dụng" table (group by `token_usage.usage_type`).

**Stack:** Backend FastAPI **:4445** (NOT :8000 — docs stale), Next.js :3000, real dev Postgres on host **:5432** (`.env` DATABASE_URL), Redis :6380 (`redis-68c` container), Zero-cache :4848 (not running — refused errors are expected noise). Logged in as `e2e-test@nowing.net` (superuser on the 5432 DB).

**Verified live:**
- `DECISION_ENABLED=true DECISION_BACKEND=mock` + `decide()` with real AsyncSession/workspace/user → `token_usage` row `usage_type='decision'`, `call_details` = backend/model/task/question_set.
- MockBackend answered `subagent_routing` set: `choice chainlens conf=0.90` with valid prob distribution.
- `/admin/telemetry` → "Đo lường Chi phí & Hàng đợi" → "Theo Loại sử dụng" renders `decision | 0 | $0.0000` — no crash on the new enum value, sorted by cost desc.
- API `GET /api/v1/admin/telemetry/llm-cost?window_hours=24` returns `decision` in `by_usage_type`.
- Console: only Zero 4848 refused (unrelated).

**Artifact:** `story-39-1-decision-usage-type.png`

## Environment discoveries (IMPORTANT — docs are stale)

- **Real dev DB is host Postgres :5432**, not docker :5434. `.env` `DATABASE_URL` points to 5432 and wins over the docker deps DB. Docker `nowing-deps-db-1` (5434) exists but holds stale data (40 token_usage rows vs 174 on 5432). Data written to the wrong DB simply never shows in the app — silent mismatch, hard to notice.
- **Backend port is :4445** — `nowing_web/.env.local` points there. AGENTS.md "backend on :8000" is outdated.
- **Alembic dead revision:** `bcf2ac4cf89d` (merge_235_and_4c04) was deleted by cleanup commits `fe12a11ed`/`404065b54`; any DB stamped with it makes `alembic upgrade`/`stamp` fail with "Can't locate revision". Workaround: `update alembic_version set version_num='239'` directly via SQL (project uses numeric revision ids; head = 239).
- **Playwright MCP quirk:** `browser_type`/`browser_fill_form` expect `target` (ref id), not `ref`.
- `nowing-deps-redis-1` exists but never runs — `redis-68c` (another container) already serves :6380 and satisfies REDIS_URL.

## Epic-39 deferred items — live verification (2026-09-22, session 2)

**Stack:** backend :4445, frontend :3000, DB :5432, logged in as e2e-test (superuser on 5432).

**Verified live:**
- Item A `required_state_keys`: `decide({}, ..., required_state_keys=("user_message",))` → `invalid_request` listing missing keys BEFORE backend call.
- Item C failed-leg telemetry: `DECISION_BACKEND=jev` (no TYPESAFE_API_KEY) → `missing_api_key` → fallback `llm_json` → `AuthenticationError` mapped `missing_api_key` → warning log + propagate. TokenUsage row persisted: `call_details={failed:true, error_code:"missing_api_key", legs:[{jev,...},{llm_json,...}]}`, tokens 0.
- Item E: `DECISION_FALLBACK_BACKEND=llm_json` default resolved (typo→"none" covered by unit test).
- Item B: `passes_negative(noul 0.05)=True`, `(0.95)=False` — inversion works.
- Browser: `/admin/telemetry` → "Đo lường Chi phí" tab → "Theo Loại sử dụng" renders `decision | 0 | $0.0000` — failed-attempt rows aggregate without breaking table. 0 relevant console errors (Zero-cache :4848 refused expected).
- Artifact: `story-39-deferred-items-telemetry.png`

**Memory-worthy facts:**
- `record_token_usage` ONLY does `session.add()` — caller owns `session.commit()`. Decision telemetry verification scripts MUST `await session.commit()` or rows roll back on session close.
- users table is `"user"` (quoted), not `users`.
- Failed-leg rows: `call_details.failed=true`, `error_code`, `legs[]` with per-leg {backend,model,latency_ms,outcome}; tokens None→0 (no fabricated spend).

## Story 39.2 — Subagent routing via DecisionService (live verify)

- Middleware path verified live: `JevRouterMiddleware(descriptors=None, ws=187, user=uuid, thread=345)` + env `DECISION_ENABLED=true DECISION_ROUTING_ENABLED=true DECISION_BACKEND=mock` → `abefore_model` returned verbatim `<jev_routing_hint>` (chainlens, 90%); second call same state → `None` (dedup).
- Telemetry row 179 persisted: `thread_id=345`, `workspace_id=187`, `call_details.task="routing"`, `question_set="subagent_routing@1.0.0"` — thread_id linkage (deferred item) confirmed end-to-end.
- `/admin/telemetry` → "Đo lường Chi phí" → "Theo Loại sử dụng" renders `decision | 0 | $0.0000`; `mock` model row appears in model table; ws 187 row present. Console: only Zero-cache :4848 refused (expected).
- Table names: `workspaces`, `new_chat_threads` (not `workspace`/`thread`).
- Artifact: `story-39-2-routing-telemetry.png`.

## Story 39.2 — Live chat flow verification (thread 346)

- Backend restarted on :4445 with `NOWING_ENABLE_JEV_ROUTER=true`, `DECISION_ENABLED=true`, `DECISION_ROUTING_ENABLED=true`, `DECISION_BACKEND=mock`.
- Sent real Vietnamese message via UI: "Nghiên cứu sâu về thị trường bất động sản quận 7 năm 2025" → thread 346.
- Backend log confirmed live middleware path: `[decision] backend=mock task=routing question_set=subagent_routing@1.0.0` then `Jev routing hint: chainlens (confidence=0.90)`.
- Telemetry persisted: `token_usage` row 180 — `usage_type=decision`, `thread_id=346`, `workspace_id=1`, `task=routing`, `qs=subagent_routing@1.0.0`, `backend=mock`. Caller-owns-commit path works in production request flow.
- ENV LIMITATION: main LLM (`claude-sonnet-5`, provider=anthropic) cannot complete — no `ANTHROPIC_API_KEY` in env/.env/.env.local and workspace 1 `connections` rows are all `fake-key` OpenAI. TitleGen retries failed with empty error; UI stayed in "running..." state. Unrelated to Story 39.2 — chat cannot produce a response without a real provider credential.
- Console: only expected Zero-cache :4848 ERR_CONNECTION_REFUSED.
- Verdict: routing middleware + DecisionService + telemetry verified end-to-end in real request flow; `task()` dispatch step unverifiable without a working LLM key.

## Story 39.3 — Entity Resolution Confidence Scoring (Jev R3) — Final Verification Gate check
- **Commit:** `ea10bd305` — 15 files, all `nowing_backend/` + `_bmad-output/`; **0 frontend files** (no `nowing_web`, no `.tsx/.ts`).
- **UI surface: NONE.** Pure backend — `entity_match_fanout` question set, `entity_resolution` service, bds dedupe `find_match_candidates`, `verify_company` Jev rescore in fuzzy band (0, 0.85).
- **Gate verdict:** Final Verification Gate NOT triggered — no page/component/dialog/stream to exercise. Browser cannot observe `decide()` calls, union-find merges, or corp rescore; there is no DOM for this layer.
- **Correct verification surface:** unit/integration — 240/240 pytest pass + ruff clean (per build session). Advisory/fail-open behavior verified by mocks, not observable in UI by design.
- **Watch item:** if a later story surfaces these verdicts (curator review queue UI, match-confidence badge on lead cards, `[entity_match]` telemetry dashboard), THAT becomes the pilot surface — re-verify then.

## Story 39.3 — Real-data proof RUN (2026-09-22, Luisphan provided TYPESAFE_API_KEY)

**Method:** `uv run python /tmp/prove_39_3*.py` in nowing_backend with env
`DECISION_ENABLED=true DECISION_ENTITY_ENABLED=true DECISION_BACKEND=jev TYPESAFE_API_KEY=...`
(key passed inline only — never written to .env or sanctum).

### BĐS fan-out (entity_match_fanout@1.0.0)
- **Live scrape run 1 (Quận 7):** 41 real listings (batdongsan 11, chotot 15, muaban 15)
  → 28 canonicals → 4 anchors/6 pairs → 4 REAL Jev calls (api.typesafe.ai 200,
  jev-1.13.0, 280-815ms, 714-954 input tokens) → all no_match conf 0.8-1.0 → 28→28.
- **Live scrape run 2 (Quận 3):** 51 listings → 26 canonicals → 4 anchors/5 pairs →
  4 calls → all no_match conf 0.96-1.0. Eyeball check: verdicts CORRECT
  (different districts/streets — "Bàn Cờ Q3" vs "Hòa Bình Q11").
- **Merge edge:** injected cross-post clone of a real harvested listing (rephrased
  title, no shared keys — the exact false-negative shape): Jev chose clone id,
  conf=1.0, action=merge → 3→2 canonicals, source_ids unioned, control stayed
  separate. Merge path + merge_group PROVEN end-to-end.

### Corp pairwise (entity_match@1.0.0, real masothue scraper)
- "Vinhomes" fuzzy 0.500 → Jev score=1.7 conf=0.55 → gate FAIL → review →
  stays manual. GATE-ON-CONFIDENCE semantics proven live (score ≠ gate input).
- "Vinhomes Đan Phượng" fuzzy 0.500 → score=1.84 conf=0.76 → auto_merge →
  is_verified=True, manual cleared, confidence field still 0.500 (fuzzy preserved).
- "Bất động sản Vinhomes Đan Phượng" fuzzy 0.500 → score=1.85 conf=0.77 → PROMOTE.
- "Công ty Vinhomes" fuzzy 0.400 → score=1.73 conf=0.6 → review → manual.
- "Đại Phát" fuzzy 0.386 → score=0.51 conf=0.24 → review (real cand was a
  DIFFERENT company "Đại Tấn Phát" — Jev caught it).
- "Taseco KCN Kim Bảng" → no candidates → conf=0 → outside band → ZERO Jev call.

### Env quirks discovered
- `.env` has NO TYPESAFE_API_KEY and NO DECISION_* flags — backend currently
  fail-open in dev. To enable: add key + `DECISION_ENABLED=true
  DECISION_ENTITY_ENABLED=true DECISION_BACKEND=jev` to `nowing_backend/.env`.
- Capability registry needs explicit imports in standalone scripts
  (`import app.capabilities.batdongsan|chotot|muaban_bds`) — routes/__init__.py
  does it in the app.
- `CorporateVerificationService(session)` — session is REQUIRED positional;
  use `app.db.base.async_session_maker`.
- muaban_bds detail-page fetch is slow (~10-30s/page via proxy); a 20-item
  district scrape takes ~7-8 min total.
- masothue search is fast (scrapling HTTP, ~2s/candidate page).

### Proof status: COMPLETE — fanout merge + separate, pairwise promote +
review-hold + out-of-band all observed with real APIs and real scraped data.

## Story 37.3 Meeting Booking — E2E verify (local BE :8000, real DB+Redis)
- Migration 240 applied (`alembic upgrade head` → head=240).
- JWT minted via `IatJWTStrategy.write_token` for admin@nowing.dev (ws 44, lead ff461979).
- Dummy `GOOGLE_CALENDAR_CONNECTOR` row seeded (id=145, dummy token) to exercise fail-open path; removed after.
- GET slots no-connector → 200 {slots:[], provider:null}; with dummy → 200 fail-open all-free ICT labels, weekends skipped.
- POST book: 502 create_failed on bad creds; retry → 502 not 409 (lock released). Validations: naive→422, past→422, bad email→422, cross-ws→403.
- Conversation (handle_turn, real Redis): propose→3 locks+state+token; confirm→booking_failed, picked lock released (3→2); reject→re-propose; reject→negotiation_limit→escalate (pause_auto_reply fired, locks→0, state cleared); post-escalate non-intent→fall-through.
- Booking_failed keeps remaining locks+state (retry-able) — noted, consistent w/ design.
