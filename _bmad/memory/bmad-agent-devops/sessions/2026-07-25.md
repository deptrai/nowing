## Session — First Breath

**What happened:** Sinh ra. Dựng sanctum, tự khảo sát địa bàn Dokploy trước khi hỏi owner
(chưa biết tên owner — config trả về "friend"). Chọn tên: Trạm.

**Key outcomes:**
- Ghi được toàn bộ ID thật của nowing: project `w-k-cq8AzW_kC9G1t5eEd`, env `v9OCYwAqJSpDHuyf4DoGf`,
  3 app + postgres + redis + searxng compose. Ba domain đều letsencrypt và đang `done`.
- Phát hiện 2 rủi ro owner chưa nêu: (1) `backups: []` — Postgres production KHÔNG có backup nào
  được cấu hình trong Dokploy, khớp cảnh báo trong README; (2) secret phơi trong env production
  (password Postgres, GitHub PAT, 3 API key) — PAT có thể rotate được ngay.
- Thấy một lệch đáng ngờ: frontend có `dockerImage: ghcr.io/deptrai/nowing-web:develop` nhưng
  `buildType: dockerfile` + branch `production`. Chưa kết luận — cần hỏi.
- Viết `incidents.md` với 8 mục: 6 bài học kế thừa từ thế hệ chainlens + 2 tự quan sát.

**Observations:** Dokploy này dùng chung với 10 project khác (có cả chainlens, vibe-trading).
Blast radius của một thao tác nhắm sai chỗ là thật → mọi tool call phải kèm ID, không dựa vào tên.

**Follow-up:** Chưa hỏi được điều quan trọng nhất — ranh giới tự quyết. Tới khi có câu trả lời,
mặc định: đọc thoải mái, không ghi gì lên production. Còn thiếu: tên owner, cửa sổ tránh deploy,
mức chấp nhận mất dữ liệu, cách họ muốn nghe tin xấu.
